using UnityEngine;

public class PlayerShoot : MonoBehaviour
{
    private void Update()
    {
        if (Input.GetMouseButtonDown(0))
        {
            // Hit code
        }
        else if (Input.GetMouseButtonDown(1))
        {
            // Shoot code
        }

        if (Input.GetMouseButtonDown(2))
        {
            // Launch code (boost up in air)
        }
    }
}
